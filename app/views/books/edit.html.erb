<h2>Editing Book</h2>
<%= form_for(@book, url:"/book_path/#{@book.id}") do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %><br>

<%= f.submit 'Update Book' %>

<p><%= link_to "Show", books_path(book.id) %>|<%= link_to "Back", books_path %></p>

<% end %>
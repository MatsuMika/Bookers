<h2>Editing Book</h2>
<%= form_for(@book) do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %><br>

<%= f.submit 'Update Book' %>

<p><%= link_to "Show", show_books_path(@book) %>|<%= link_to "Back", show_books_path(@book) %></p>

<% end %>